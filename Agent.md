# AGENT.md — ClaimAgent Hospital OPD

## สถานะปัจจุบันของโปรเจกต์

โปรเจกต์นี้เป็นเว็บแอป **vanilla HTML/CSS/JS** (ไม่มี framework, ไม่มี build step) รันได้ด้วยการเปิด `index.html` ตรงๆ

ไฟล์ `index.html` เดิมมีขนาดใหญ่มาก (60,000+ บรรทัด) โดยโค้ดส่วนใหญ่ของทุกเมนูรวมกันอยู่ใน `<script>` ก้อนเดียว (core/legacy script) เราเริ่มแยกโค้ดออกเป็นไฟล์ย่อยทีละส่วนแบบค่อยเป็นค่อยไป (ดูหัวข้อ "ประวัติการแตกไฟล์")

**กติกาสำคัญ: อย่าแก้ core/legacy script ในไฟล์ index.html โดยตรงถ้าไม่จำเป็น** — ให้เขียนโค้ดใหม่หรือหน้าใหม่แยกเป็นไฟล์ `.js` ต่างหากเสมอ ตามรูปแบบด้านล่าง เพื่อไม่ให้ไฟล์หลักบวมขึ้นอีก และเพื่อให้ debug ง่าย

## โครงสร้างไฟล์

```
project/
├── index.html                          # ไฟล์หลัก: layout, sidebar, core/legacy script (ของเดิม)
├── AGENT.md                            # ไฟล์นี้
├── css/
│   └── (ไฟล์ CSS แยกที่จะเพิ่มในอนาคต)
└── js/
    └── menu-billing-hospital-review.js # ตัวอย่างเมนูที่แยกไฟล์แล้ว (วางบิล > เคลมโรงพยาบาล > ตรวจสอบรายการ)
```

## วิธีดูว่ามีเมนู/หน้าอะไรบ้าง

เปิด `index.html` แล้วหาคอมเมนต์ `<script src="js/...">` — ทุกไฟล์ที่ถูก reference ไว้แบบนี้คือเมนูที่แยกไฟล์แล้ว วางเรียงต่อเนื่องกันในไฟล์หลัก พร้อมคอมเมนต์บอกชื่อเมนูกำกับไว้เหนือแต่ละ `<script src>` เช่น:

```html
<!-- วางบิลเคลม > เคลมโรงพยาบาล > ตรวจสอบรายการวางบิล: แยกไปที่ js/menu-billing-hospital-review.js -->
<script src="js/menu-billing-hospital-review.js"></script>
```

ส่วนเมนูที่ยังไม่ได้แยก (ยังอยู่ใน core/legacy script) ให้หาใน `<script>` ก้อนใหญ่ต้นๆ ของไฟล์ (ค้นด้วยชื่อฟังก์ชัน เช่น `function show...Page(`)

## วิธีเพิ่ม "หน้าใหม่" หรือ "เมนูใหม่"

### ขั้นตอน

1. **สร้างไฟล์ JS ใหม่** ใน `js/` ตั้งชื่อสื่อความหมาย รูปแบบ: `menu-<ชื่อกลุ่มเมนู>-<ชื่อหน้าย่อย>.js`
   เช่น `menu-billing-customer-review.js`, `menu-report-monthly-summary.js`

2. **เขียนโค้ดในไฟล์ใหม่แบบห่อด้วย IIFE เสมอ** เพื่อไม่ให้ตัวแปรชนกับเมนูอื่น:

   ```js
   /* ============================================================
      menu-<ชื่อไฟล์>.js
      <อธิบายว่าเมนูนี้ทำอะไร / อยู่ใต้เมนูไหนใน sidebar>
      ============================================================ */
   (function () {
     "use strict";

     // เขียนฟังก์ชัน render, event handler ฯลฯ ของหน้านี้ที่นี่
     function showMyNewPage() {
       hideAllPages();               // ใช้ helper กลางที่มีอยู่แล้วในไฟล์หลัก
       document.getElementById("myNewPage").classList.remove("hidden");
     }

     // เปิดให้ index.html หรือไฟล์อื่นเรียกใช้ได้ผ่าน window
     window.showMyNewPage = showMyNewPage;
   })();
   ```

3. **เพิ่ม `<section>` ของหน้าใหม่ใน `index.html`** (ใน container ที่รวมทุกหน้า เช่นเดียวกับหน้าอื่นๆ ที่มีอยู่):

   ```html
   <section id="myNewPage" class="page hidden">
     <!-- เนื้อหาหน้าใหม่ -->
   </section>
   ```

4. **เพิ่มเมนูใน sidebar** (`<nav>`/`<aside>` ในไฟล์หลัก) โดย copy pattern ของเมนูที่มีอยู่แล้ว แล้วเปลี่ยน `onclick` ให้เรียกฟังก์ชันจากข้อ 2:

   ```html
   <li>
     <a href="#" onclick="showMyNewPage(); return false;">ชื่อเมนูใหม่</a>
   </li>
   ```

5. **โหลดไฟล์ JS ใหม่ใน `index.html`** วางไว้ท้ายๆ ก่อน `</body>` (หลัง core/legacy script เสมอ เผื่อโค้ดใหม่ต้องเรียกใช้ helper function จาก core):

   ```html
   <!-- <ชื่อเมนูใหม่>: js/menu-<ชื่อไฟล์>.js -->
   <script src="js/menu-<ชื่อไฟล์>.js"></script>
   ```

6. **ทดสอบในเบราว์เซอร์จริง** — เปิด `index.html`, กดเมนูใหม่, เช็ค console (F12) ว่าไม่มี error สีแดง

### ถ้าหน้าใหม่มี CSS เฉพาะตัวเยอะ

สร้างไฟล์ `css/menu-<ชื่อไฟล์>.css` แยกต่างหาก แล้วโหลดใน `<head>` ของ `index.html`:
```html
<link rel="stylesheet" href="css/menu-<ชื่อไฟล์>.css">
```
ไม่ต้องฝัง `<style>` ปนอยู่กลางไฟล์ index.html แบบเมนูเก่า

## กติกาการตั้งชื่อ

| สิ่งที่ตั้งชื่อ | รูปแบบ | ตัวอย่าง |
|---|---|---|
| ไฟล์ JS ของเมนู | `menu-<กลุ่ม>-<หน้าย่อย>.js` | `menu-billing-hospital-review.js` |
| ฟังก์ชันแสดงหน้า | `show<ชื่อหน้า>Page()` | `showBillingHospitalReviewPage()` |
| id ของ `<section>` | `<ชื่อหน้า>Page` | `billingHospitalReviewPage` |

## Helper function กลางที่มีอยู่แล้ว (เรียกใช้ได้จากทุกไฟล์)

ฟังก์ชันเหล่านี้ประกาศอยู่ใน core/legacy script ของ `index.html` — ไฟล์เมนูใหม่เรียกใช้ได้เลยโดยไม่ต้องประกาศซ้ำ (เพราะทุก `<script>` ในหน้าเดียวกันใช้ global scope ร่วมกัน):

- `hideAllPages()` / `hideClaimPages()` — ซ่อนทุกหน้าก่อนแสดงหน้าใหม่
- `money(n)` — format ตัวเลขเป็นรูปแบบเงิน
- (เพิ่มรายการที่ใช้บ่อยตรงนี้เมื่อเจอเพิ่ม)

> ⚠️ ก่อนตั้งชื่อฟังก์ชัน/ตัวแปรใหม่ ให้ `grep` หาชื่อนั้นใน `index.html` ก่อนเสมอ เพื่อเช็คว่าชนกับของเดิมหรือไม่ (ไฟล์นี้ใช้ global scope ร่วมกันทั้งหมด ชื่อซ้ำ = ทับของเดิมโดยไม่รู้ตัว)

## ประวัติการแตกไฟล์

| วันที่ | เมนูที่แยก | ไฟล์ |
|---|---|---|
| รอบแรก | วางบิลเคลม > เคลมโรงพยาบาล > ตรวจสอบรายการวางบิล | `js/menu-billing-hospital-review.js` |

> เมนูอื่นๆ ที่ยังไม่ได้แยก (แจ้งเคลม, พิจารณาเคลม, จัดการเงินเคลม ฯลฯ) ยังอยู่ใน core/legacy script ของ `index.html` — การแยกเมนูเหล่านี้ต้องทำอย่างระมัดระวัง เพราะฟังก์ชันของหลายเมนูใช้ตัวแปรร่วมกันอยู่ในสโคปเดียวกัน แนะนำให้ทำทีละเมนู พร้อมทดสอบในเบราว์เซอร์จริงทุกครั้งก่อนแตกเมนูถัดไป

## ข้อจำกัดที่ควรรู้

- ไม่มี build step / bundler — ไฟล์ .js ทุกไฟล์ต้องรันได้ตรงๆ ในเบราว์เซอร์ (ES5/ES6 ธรรมดา, ไม่มี `import`/`export` แบบ ES module)
- ลำดับการโหลด `<script>` มีผล — ถ้าไฟล์ใหม่เรียกใช้ฟังก์ชันจากไฟล์อื่น ต้องวาง `<script src>` ของไฟล์นั้นไว้ **ก่อน** เสมอ
- ทดสอบด้วยการเปิดไฟล์ในเบราว์เซอร์จริงทุกครั้งหลังแก้ไข ไม่มีระบบทดสอบอัตโนมัติ
